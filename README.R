## set the path of the data

# get the activity name and features name from the features.txt file
# for name the column of the data

# get the activity name and Activity name from the Activity.txt file
# from names the activity



#tidy train Data and test data
# reading the subject_train id from the file subject_train.txt
# read training measurment for each observation from x_train.txt
# read traing label of each ouput acitivity for each observation
  
# merge the traing data with test data
# and select the mean and std of each measurment


##  tidy data set with the average of each variable for each activity and each subject.

# by split the data by subject id then by acitvity id then obtain mean for each measurment
